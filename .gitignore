# Data (12 GB KITTI + raw) -- never commit
data/kitti/
data/*.zip
data/kitti_sample/*.zip
*.bin
*.kitti

# Training artifacts
checkpoints/
*.pt
onnx/
*.onnx
benchmark_results.csv

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# Env
.env
venv/
.venv/

# Personal notes / drafts (local only, not for the remote)
fusion*.md
sess*.md
.zcode/

# Large binary artifacts (regenerable, not source)
deploy/*.png
notebooks/*.png